apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: orion-api
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: orion-api
  policy:
    semver:
      range: ">=0.0.0"

